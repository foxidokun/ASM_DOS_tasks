default:
	@ echo "#################################################"
	@ echo "#                SAFETY MESSAGE                 #"
	@ echo "# Be carefull, don't run make without arguments #"
	@ echo "#################################################"

clean:
	rm -rf *.LST *.COM *.MAP *.OBJ